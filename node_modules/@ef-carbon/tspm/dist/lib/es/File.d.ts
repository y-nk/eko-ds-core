/// <reference types="node" />
/// <reference types="node" />
import { PathLike } from "fs";
import Export from "./Export";
import Import from "./Import";
import Require from "./Require";
import Base, { IDerivedOptions as IBaseOptions } from "../File";
export declare type IOptions = IBaseOptions;
export default class File extends Base<Import | Require, Export> {
    private program;
    constructor({ ...options }: IOptions);
    private readonly ast;
    imports(): AsyncIterableIterator<Import | Require>;
    exports(): AsyncIterableIterator<Export>;
    write(path?: PathLike | number, options?: {
        encoding?: string | null;
        mode?: number | string;
        flag?: string;
    } | string | null): Promise<void>;
}
