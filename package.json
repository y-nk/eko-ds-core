{
  "name": "eko-ds-core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "jest --watch --verbose",
    "test": "jest --verbose",
    "build": "tsc --project . && ef-tspm",
    "deploy": "npm run test && npm run build && cp package.json lib/ && npm publish lib/ --dry-run && rm -rf lib/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@ef-carbon/tspm": "^2.2.5",
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.17",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.3",
    "csv-parser": "^2.3.2"
  }
}
